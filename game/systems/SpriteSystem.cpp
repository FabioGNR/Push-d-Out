#include "SpriteSystem.h"

game::systems::SpriteSystem::SpriteSystem() = default;

void game::systems::SpriteSystem::update(double frameTime)
{
    // BaseSystem::update(frameTime);

    // Do something
}